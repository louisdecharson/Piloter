extends layout

block content
        div(class='head-space')
        h2 Posts récents
        hr
        each post in posts
            div(class='post')
                    a(href="/posts/" + post.slug class='post-title')= post.title
                    div(class='date')= post.date
                    div(class='post-abstract')= post.abstract
                    
